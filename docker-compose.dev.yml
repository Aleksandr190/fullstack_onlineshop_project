version: '3.8'

services:
  frontend:
    build:
      context: ./frontend_project
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend_project:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    stdin_open: true
    tty: true